# Tâches dev — 22/09/2026

Sources : COPROJ du 17/09 (4 bugs prioritaires) + actions manuelles héritées du 04/09,
jamais exécutées. Préprod `megaserviceshop.nuttyguru.com`, PrestaShop 8.2.

---

## ✅ Accès serveur — débloqué le 22/09

La clé `~/.ssh/drpetrea_preprod` (utilisateur Plesk `nuttyguru.com_j8g81qmwucn`) **ouvre
déjà l'accès à Megaservice** : même souscription que le site drpetrea. Aucune clé à créer.

```
ssh -i ~/.ssh/drpetrea_preprod nuttyguru.com_j8g81qmwucn@93.90.201.182
cd /var/www/vhosts/nuttyguru.com/megaserviceshop.nuttyguru.com
/opt/plesk/php/8.2/bin/php scripts/cli/debug_product_availability.php <id_product>
```

`php` n'est pas dans le PATH : utiliser `/opt/plesk/php/8.2/bin/php`.
`scripts/` n'est **pas déployé par la CI** (elle ne pousse que `megaservice/`, `override/`
et `modules/`) — les scripts de diagnostic sont à envoyer par `scp` au besoin, et à
déposer dans `scripts/cli/`, **jamais à la racine web**.

Reste inaccessible : le front en navigation (Basic Auth) et phpMyAdmin.
Les requêtes SQL passent désormais par `php -r` en SSH.

---

## 1. ~~Statut « en stock constructeur » mais non commandable~~ — ✅ CORRIGÉ le 23/09

**Ce n'était pas un bug de règle : le blocage vient d'une demande explicite du client
(spec §13). Le défaut était le libellé.**

**Correction livrée** : dans la branche `backorder`, quand la date est future, le texte
devient « **Commandable à partir du [date]** » au lieu de « Dispo. le [date] ». La règle
`msCommandable` n'est pas touchée — aucun produit ne change de commandabilité. Voir
spec §15.

Reste ouvert : l'arbitrage « précommande » du §3 cas 4, qui lui porte sur la règle.

Règle en vigueur ([product.tpl:88-89](../megaservice/templates/catalog/product.tpl#L88-L89)
et son équivalent dans la miniature) :

```smarty
{assign var='msDateFuture'  value=($product.available_date > $msToday)}
{assign var='msCommandable' value=($product.add_to_cart_url && !$msDateFuture)}
```

Le bouton d'achat est **délibérément bloqué tant que la date de réassort annoncée
n'est pas atteinte** (commit `6b070e6`).

### Vérification sur la référence de test `00010000348` (id 94496)

| Champ | Valeur |
|---|---|
| `active` / `available_for_order` / `show_price` | 1 / 1 / 1 |
| `quantity` | 0 |
| `available_date` | **2026-09-18** |

Le COPROJ s'est tenu le **17/09**, soit la veille. La date était donc dans le futur →
bouton désactivé, pendant que le bandeau affichait « Dispo. le 18/09/2026 ». Le client
a vu une promesse de disponibilité à côté d'un bouton verrouillé sans explication.

### Le vrai sujet : 1 874 produits dans cet état aujourd'hui

| Situation (au 22/09) | Produits |
|---|---|
| Commandables, date de réassort **future** → bouton bloqué | **1 874** |
| Commandables, date passée ou absente → bouton actif | 44 806 |

Ce n'est donc pas un cas isolé, et ça se reproduira en permanence.

### Ce qui est à trancher, pas à coder

La question rejoint l'arbitrage « précommande » déjà en attente (spec §3 cas 4) :

- soit on **assume le blocage** et on explique le refus au client (« commandable à partir du 18/09 » plutôt qu'un bouton mort),
- soit on **ouvre la précommande** sur la date annoncée.

**Aucune modification de code avant cet arbitrage** — la spec l'interdit explicitement.
Prévoir de confirmer au client que c'est bien ce cas qu'il a vu au COPROJ.

---

## 2. Régression JS — filtres PowerParts inactifs

**Priorité 2** — je peux préparer le terrain sans accès, la reproduction reste bloquée.

### Ce que dit l'historique Git

Les fichiers de facettes n'ont **pas bougé depuis avril** :

| Fichier | Dernier commit |
|---|---|
| `_partials/facets.tpl` | 2026-04-22 |
| `assets/js/filters.js` | 2026-04-27 |

La régression ne vient donc probablement **pas d'une modification des filtres
eux-mêmes**, mais d'un effet de bord. Piste principale : le bundle `theme.js` a été
recompilé plusieurs fois depuis (27/08, 28/08, 04/09) et `per-page.js` y a été ajouté
le 28/08. Une erreur JS levée par un module chargé **avant** `filters.js` interromprait
le bundle et désactiverait silencieusement les écouteurs de filtres — ce qui collerait
avec « certains filtres » plutôt que tous.

- [ ] Reproduire sur une page PowerParts, console ouverte — **relever l'erreur JS exacte au clic** *(accès préprod)*
- [ ] Noter si la panne touche tous les filtres ou une famille précise
- [ ] Selon l'erreur : corriger la source, puis valider sur 3 catégories PowerParts distinctes

**Sans le message d'erreur de la console, je ne peux pas aller plus loin** — le code
des filtres est intact, la cause est ailleurs et seule l'exécution la révèle.

---

## 3. Rattachement des images — produits variables

**Le brief le classe « correction directe ». Ce n'est pas possible en l'état.**

### Pourquoi

Le script de rattachement des images **n'existe pas dans ce dépôt**. Vérifié par
recherche exhaustive : aucun code produit d'images catalogue dans `modules/`,
`override/` ou `scripts/`. Les seules correspondances sont les uploads de visuels
microfiches/motos en back-office, sans rapport.

L'import catalogue est assuré par **`ba_importer` v1.1.33**, module tiers présent
uniquement sur le serveur — le même que celui qui recrée les caractéristiques en
doublon (cf. TECH_DEBT 🔴).

Les quatre pistes du brief — casse, réf en `X`, extraction depuis le nom de fichier,
photos multiples — décrivent donc le comportement d'un code **qu'on ne possède pas
et qu'on ne peut pas corriger**.

### Deux voies possibles

**A. Régler `ba_importer`** — inspecter sa configuration d'import d'images sur le
serveur, comme on l'a fait pour `fea_exist`. Le moins de code, mais dépend de ce que
le module expose comme réglages.

**B. Écrire notre propre script de rattachement** — un CLI dans `scripts/cli/` qui lit
le fichier d'import, résout la référence parent en comparaison **insensible à la
casse**, et attache les N photos au parent avec une en cover. On maîtrise le
comportement, on contourne `ba_importer`. Faisable entièrement d'ici.

- [ ] **Arbitrage à rendre : voie A ou voie B ?**
- [ ] Si B : me fournir un extrait du fichier d'import (colonnes + quelques lignes) et la convention de nommage des fichiers photo

Cas de test une fois corrigé : produit `143456` / `3RB26003390X` → 3 images sur le
parent, visuel hérité sur les 6 tailles.

---

## 4. Parcours d'ajout au panier des pièces d'un kit

**Priorité 4** — le plus complexe, et il dépend de l'issue du point 1.

Cas remonté : un kit dont les 2 composants sont indisponibles produit un état de
bouton/quantité ambigu — ni actif, ni proprement désactivé.

- [ ] Lire le code du parcours kit et cartographier le comportement actuel par état de disponibilité *(faisable d'ici)*
- [ ] Reproduire les 3 états : tous dispo / mixte / tous indispo *(accès préprod)*
- [ ] Corriger l'état « tous indisponibles » : désactivation propre et explicite
- [ ] Garantir qu'un composant disponible reste ajoutable même si les autres ne le sont pas

Hors périmètre : la terminologie affichée (« remplacée par » / « composé de »), qui
fait l'objet d'un brief séparé.

---

## Actions manuelles en attente — indépendantes des 4 bugs

Aucune n'est exécutable depuis le poste de dev.

- [x] ~~**`2026-09-04_fix_show_price_defaut_import.sql`**~~ — **SANS OBJET, mesuré le 22/09.** Sa cible (`available_for_order=1 AND show_price=0`) vaut **0**. Le module `megaservice_availability` a corrigé les 46 680 produits commandables ; les 1 236 restants à `show_price=0` sont tous non commandables, prix masqué légitimement. Ne pas exécuter.
- [x] ~~Vérifier l'installation de `megaservice_availability`~~ — **confirmée effective** par la mesure ci-dessus.
- [ ] **`fea_exist` → 1 sur les 6 profils `ba_importer`** — seule action qui tarit la source des doublons de caractéristiques
- [ ] **Rejouer `data/sql/2026-08-28_dedupe_feature_values.sql`** après chaque import catalogue, tant que le point ci-dessus n'est pas fait
- [ ] **Renommer les 25 catégories microfiches** `TODO_xxx` → français (`UPDATE` prêt dans `docs/microfiches/STATUS.md` §5)

---

## Ordre révisé

Le brief suggérait 3 → 2 → 1 → 4. Révisé, parce que le point images n'est pas
attaquable directement :

1. **Statut non commandable** — un script CLI et une requête suffisent à trancher ; peut se révéler déjà résolu
2. **Régression JS** — bloquée sur un message de console, rien d'autre à faire avant
3. **Images** — arbitrage A/B à rendre avant toute ligne de code
4. **Kit** — lecture de code possible dès maintenant, correction après le point 1

---

## Ce que je peux démarrer sans attendre

- [ ] Cartographier le parcours kit dans le code (point 4, première étape)
- [x] ~~Défaut CSS du bouton « Ajouter »~~ — **déjà corrigé** par `1418a06` (modificateur grisé `.ms-product-card__add--disabled` + libellé « VOIR LE PRODUIT »). Ma lecture du 22/09 était fausse : `8b7994b` documente la résolution, il ne signale pas un reste à faire.
- [ ] Mettre à jour la fiche TECH_DEBT 🔴 « préprod écrasée par `main` », périmée depuis les merges du 28/08
